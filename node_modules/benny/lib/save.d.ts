import { Suite } from 'benchmark';
import { SaveOptions, Config } from './internal/common-types';
declare type Save = (options?: SaveOptions) => (config: Config) => Promise<(suiteObj: Suite) => Suite>;
declare const save: Save;
export { save, Save };
export default save;
