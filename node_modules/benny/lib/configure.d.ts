import { Config } from './internal/common-types';
declare type ConfigureResult = {
    name: 'config';
    entries: Config;
};
declare type Configure = (options: Config) => ConfigureResult;
declare const configure: Configure;
export { ConfigureResult };
export default configure;
