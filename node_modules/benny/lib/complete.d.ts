import { Suite } from 'benchmark';
import { Summary, Config } from './internal/common-types';
declare type CompleteFn = (summary: Summary) => any;
declare type Complete = (fn?: CompleteFn) => (config: Config) => Promise<(suiteObj: Suite) => Suite>;
declare const complete: Complete;
export { complete, Complete };
export default complete;
